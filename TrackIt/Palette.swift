import SwiftUI

enum Palette {
    static let backgroundTop = Color(red: 0.05, green: 0.07, blue: 0.12)
    static let backgroundMid = Color(red: 0.07, green: 0.10, blue: 0.16)
    static let backgroundBottom = Color(red: 0.06, green: 0.08, blue: 0.13)
    static let card = Color(red: 0.09, green: 0.12, blue: 0.20)
    static let stroke = Color.white.opacity(0.1)
    static let primary = Color.white
    static let secondary = Color.white.opacity(0.65)
    static let accent = Color(red: 0.36, green: 0.67, blue: 1.0)
    static let accentAlt = Color(red: 0.53, green: 0.62, blue: 0.98)
    static let mutedFill = Color.white.opacity(0.06)
}
